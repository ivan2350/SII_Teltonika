from pymodbus.client import ModbusTcpClient

client = ModbusTcpClient("127.0.0.1", port=502)

client.connect()

rr = client.read_discrete_inputs(0, 2, unit=32)

print("Resultado:", rr.bits if rr else "Error")

client.close()
